rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow access to app_users collection
    match /app_users/{userId} {
      allow read, write: if true;
    }
    
    // Allow access to requests / transactions collection
    match /requests/{requestId} {
      allow read, write: if true;
    }

    // Allow access to notifications collection
    match /notifications/{notificationId} {
      allow read, write: if true;
    }

    // Allow access to group payments collection
    match /groupPayments/{paymentId} {
      allow read, write: if true;
    }

    // Allow access to balance transactions collection
    match /balance_transactions/{transactionId} {
      allow read, write: if true;
    }

    // Allow access to balance payout requests collection
    match /balance_requests/{requestId} {
      allow read, write: if true;
    }

    // Legacy fallback user documents
    match /users/{userId} {
      allow read, write: if true;
      match /{allSubcollections=**} {
        allow read, write: if true;
      }
    }
  }
}
